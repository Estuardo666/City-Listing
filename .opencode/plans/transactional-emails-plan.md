# Transactional emails plan

The email integration uses the existing Resend adapter through a server-side
environment variable. Credentials are intentionally omitted from this plan;
configure them only in the hosting provider and rotate any value previously
shared outside the secret manager.
